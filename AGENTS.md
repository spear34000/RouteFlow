# RouteFlow — AGENTS.md

이 파일은 에이전트 작업 기준용 내부 문서다.

- 사용자 문서는 [`README.md`](./README.md)와 [`docs/`](./docs/README.md)를 기준으로 본다.
- 제품 핵심 메시지는 `REST처럼 쓰는데, DB 변경이 생기면 구독 중인 클라이언트에 자동으로 푸시된다.`이다.
- `docs/`에는 RouteFlow 사용법만 둔다. 작업용 메모를 섞지 않는다.
